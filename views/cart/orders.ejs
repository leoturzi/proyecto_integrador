<%- include('../partials/head', {title: 'Cart Orders'})%> <%-
include('../partials/header')%> <%- include('../partials/nav')%>
<div class="container--fixed">
    <main>
        <div class="cart-orders">
            <table class="cart-orders__table">
                <caption class="cart-orders__caption">Orders</caption>
                <tbody class="cart-orders__tbody">
                    <tr class="cart-orders__row">
                        <th>Fecha</th>
                        <th>Domicilio</th>
                        <th>Método de pago</th>
                        <th>Total</th>
                        <th>Detalles</th>
                    </tr>
                </tbody>
                <!-- IF orders, render tableRow(s)-->
            </table>
                <!--ELSE  empty order table-->
                <a class="btn btn-back" href="/users/profile">Volver</a>
        </div>
    </main>
</div>
<%- include('../partials/footer') %>
